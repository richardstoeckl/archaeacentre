% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/getPfamAnnotationForPDBFilenames.R
\name{get_pfam_annotation_for_targets}
\alias{get_pfam_annotation_for_targets}
\title{Retrieve Pfam Annotations for PDB Targets from RCSB API}
\usage{
get_pfam_annotation_for_targets(targets, batch_size = 1000)
}
\arguments{
\item{targets}{A character vector of PDB filenames with assembly and chain information as returned by Foldseek in the "target" column.}

\item{batch_size}{An integer specifying the number of targets to process in one batch (default: 1000).
The API calls to the RCSB server are made in batches to avoid overloading the server.}
}
\value{
A data frame with columns:
\itemize{
\item \code{target}: The original target name from Foldseek output.
\item \code{rcsb_id}: The RCSB identifier for the matched polymer entity. Note: This uses the "label_asym_id" instead of the "auth_asym_id" used in the target name.
\item \code{title}: The title of the PDB entry associated with the entity.
\item \code{pfam_description}: The description of the Pfam family associated with the entity.
}
}
\description{
This function retrieves Pfam domain annotations for a given set of PDB target structures.
The targets are expected to be results from a local Foldseek search against the PDB database.
The function extracts the relevant PDB assembly and chain identifiers, queries the RCSB API,
and returns the Pfam descriptions associated with the identified polymer entities.
}
\details{
The function operates in the following steps:
\enumerate{
\item Extracts PDB assembly and chain IDs from the target names.
\item Queries the RCSB API to retrieve corresponding polymer entity identifiers.
\item Filters results to match Foldseek output.
\item Retrieves Pfam annotations for the identified entities.
\item Merges results into a structured data frame.
}

Internally, the function calls:
\itemize{
\item \code{get_assembly_id_from_target()}: Extracts assembly ID from PDB target name.
\item \code{get_auth_asym_ids()}: Retrieves chain identifiers and RCSB polymer entity IDs.
\item \code{get_pfam_annotation()}: Queries the RCSB API for Pfam domain information.
}
}
\section{Background}{

The \href{https://www.rcsb.org/}{Research Collaboratory for Structural Bioinformatics (RCSB)} provides structural and functional
annotations for macromolecules stored in the Protein Data Bank (PDB). \href{https://github.com/steineggerlab/foldseek}{Foldseek} is a fast search tool
for comparing protein structures. When searching for similar structures in the PDB, Foldseek returns a table which contains the "target" column.
In the case of searches against the PDB, this target column contains the PDB filename of the hit, which is not easily interpretable.

This function automates the extraction of Pfam domain annotations for these target PDB filenames returned by Foldseek,
using the \href{https://data.rcsb.org/index.html#gql-api}{RCSB GraphQL API}.
}

\examples{
\dontrun{
targets <- c("1ABC_assembly1.cif_A", "2XYZ_assembly2.cif_B")
pfam_results <- get_pfam_annotation_for_targets(targets, batch_size = 1000)
head(pfam_results)
}

}
